# Ai-knowledge 目前狀態

> 本文件是新 AI 接手時的「目前狀態快照」。
> 它只描述現在，不取代 `規則.md`、`待辦清單.md` 或歷史文件。
> 若本文件與實際檔案樹衝突，以實際檔案為準，並將衝突列入更新驗證。

## 1. 當前定位

Ai-knowledge 是一套供單人工作室長期使用的 AI 工程知識庫。

核心目的：

> 第一次遇到問題時研究、驗證並保存可重用內容；下一次遇到類似問題時優先重用，降低重新研究與重新理解成本。

核心架構：

```text
知識
↓
系統
↓
方案
↓
軟體／工具
↓
應用
```

「記憶」是維持跨工作階段連續性的內容用途，不等同於知識；目前主要由 `目前狀態.md`、`交接資料.md`、工作紀錄及相關文件共同承載。

## 2. 目前已建立的主要層級

### 知識層 `0-知識/`

目前已形成的主要功能分類：

- 上下文
- 分類
- 搜尋
- 更新
- 比較
- 管理
- 系統
- 證據

其中已建立搜尋／研究相關知識、證據與可信度相關知識、更新相關知識，以及 AI 上下文與交接相關知識。

### 系統層 `1-系統/`

目前已建立或正在使用的主要系統包括：

- 資料分類與組織系統
- 資料探索與搜尋系統
- 來源與證據分析系統
- 知識比較與衝突分析系統
- 知識更新與狀態管理系統
- 知識整合系統
- AI 上下文與交接系統
- 方案管理系統
- 研究問題與搜尋策略系統
- 研究證據與採用判斷系統
- 研究結果整合系統
- 更新影響分析與成本判斷系統
- 更新監控與觸發系統

部分系統仍處於「文件已建立、等待或正在實際驗證」狀態；不能因文件存在就視為已完成驗證。

### 方案層 `2-方案/`

目前主要方案包括：

- AI 知識管理方案
- 搜尋研究方案
- 整體系統更新方案（擴展版）

目前整體系統更新方案已完成第一版功能、所需知識、系統與方案的基本建構，進入實際驗證階段。

「方案」是多個系統為特定目的組合而成的工作方法，不等同於單一系統。

## 3. 目前最重要的工作

目前主線已進入「整體系統更新方案」的實際運作驗證。

本輪執行順序為：

```text
同步目前狀態
↓
檢查 AI 模型／服務是否有新變更
↓
分析模型變更是否影響目前知識／系統／方案
↓
選定第一個既有方案／核心分類架構進行更新驗證
↓
執行變更偵測 → 比較 → 影響分析 → 更新必要性判定
↓
版本／廢棄／系統同步／重新搜尋／一致性驗證
```

### 整體系統更新方案目前已建立的能力

- 變更來源與更新觸發判定
- 依使用頻率安排更新檢查
- AI 模型更新檢查
- 新建系統／方案／軟體時重新確認所引用舊知識
- 新舊知識比較與關係判定
- 版本建立與對齊
- 舊版本移入 `廢棄/`
- 直接影響與逐層影響分析
- 更新必要性與更新成本判斷
- 系統／方案同步
- 更新後重新搜尋、確認與一致性驗證
- 更新決策與停止原因記錄

## 4. 本輪模型更新檢查結果

截至 2026-09-16，已重新檢查 OpenAI 官方模型發布與 ChatGPT 模型說明。

已確認存在的近期變更包括：

- GPT-5.6 Sol 已於 2026-07-09 開始在 ChatGPT 推出。
- 2026-08-18 有模型規格（Model Spec）更新。
- 2026-08-06 的 ChatGPT 更新包含 GPT-5.6 Sol 的更新，以及 GPT-5.6 Luna 成為 Free／Go 使用者預設模型的安排。
- 官方目前的 ChatGPT 模型說明亦列出 GPT-6 Pro（由 GPT-6 Astra 提供支援）與 GPT-5.6 系列的產品／方案差異。

模型變更目前判定為「需要更新模型檢查知識的實際案例」，但不因此判定整個 Ai-knowledge 必須重建。下一步是檢查目前工作實際依賴的模型能力，再決定哪些系統／方案需要重新驗證。

## 5. 分類／分層方案目前狀態

目前正式核心模型位於：

`0-知識/分類/資料層級與分類.md`

其核心不是建立單一路徑的「資料 → 資訊 → 知識 → 智慧」階梯，而是將內容／語義、形成／支撐、承載形式、工作結果、管理屬性分開處理。

目前正式分類目錄仍以功能為主要維度：

```text
0-知識/
├─ 管理/
├─ 分類/
├─ 搜尋/
├─ 證據/
├─ 比較/
├─ 更新/
├─ 系統/
└─ 上下文/
```

目前本次更新的重點不是重新發明分類樹，而是驗證：

1. 資料層級與分類模型是否仍符合目前整體架構。
2. 分類原理、分類維度、分類與目錄是否與核心模型一致。
3. `0-知識` 的功能導向目錄是否仍是最低成本的實作。
4. 相關系統與方案是否仍引用目前版本，而非歷史版本。
5. 實際任務是否能依分類快速定位、理解與重用資料。

## 6. 文件同步狀態

目前主要架構文件已同步理解，但 `檔案索引.md` 仍需補登近期新增／實質更動檔案；在索引完成前，不宣稱 repository 已完全同步。

已知近期需要補登至少包括：

- `0-知識/更新/更新觸發與變更偵測原理.md`
- `0-知識/更新/更新頻率與審查週期原理.md`
- `0-知識/更新/AI模型更新檢查原理.md`
- `1-系統/更新監控與觸發系統.md`
- `2-方案/整體系統更新方案.md`
- 本次後續實質更新的檔案

## 7. 狀態判定原則

文件存在 ≠ 系統已驗證。

系統文件成立 ≠ 方案已驗證。

方案成立 ≠ 實際應用已證明有效。

外部研究成立 ≠ 本 repository 已實際驗證。

目前狀態若無法由文件與實際檔案共同確認，標記為待判定，不自行補完。

## 8. 文件分工

```text
README.md
= 最短入口

說明.md
= 整體目的、架構與閱讀方式

目前狀態.md
= 現在到底做到哪裡、哪些已成立、哪些仍待驗證

規則.md
= 系統正式怎麼運作

待辦清單.md
= 下一步要做什麼

交接資料.md
= 為什麼走到現在、重要歷史決策與架構轉折

檔案索引.md
= 協助定位近期變更與文件

更新紀錄/
= 已完成更新的歷史記錄
```

任何文件都不應同時承擔上述全部責任。

## 9. 接手後的最小讀取順序

新 AI 不需要先完整閱讀所有文件。

```text
README.md
↓
說明.md
↓
目前狀態.md
↓
規則.md
↓
待辦清單.md
↓
依當前任務讀取相關系統／方案／知識
```

如果發現以上文件互相矛盾：

```text
停止猜測
↓
檢查實際檔案樹
↓
確認最新提交與更新紀錄
↓
判斷哪一份文件已過時
↓
必要時進入整體系統更新方案
```

## 10. 目前主要未決事項

1. 完成整體系統更新方案的第一個完整實際案例。
2. 補同步 `檔案索引.md`，並執行隔日檢索規則。
3. 處理舊 `0-knowledge/` 與正式 `0-知識/` 並存問題。
4. 實際驗證「無記憶 AI 僅依 repository 接手」是否能在有限閱讀下完成下一個任務。
5. 將交接驗證結果回饋至 AI 上下文與交接系統。
6. 後續再處理「融合、疊代」方案；本輪不把融合／疊代混入更新方案。

## 11. 本文件的維護規則

本文件只保存「現在最重要、能直接影響接手與下一步判斷」的資訊。

測試過程、歷史決策與詳細修改紀錄留在 `更新紀錄/` 或既有專門文件，不把本文件變成整個 repository 的摘要副本。

每次重大架構、系統、方案或工作主線變更後，應檢查本文件是否仍然正確。
