# Ai-knowledge 目前狀態

> 本文件是新 AI 接手時的「目前狀態快照」。
> 它只描述現在，不取代 `規則.md`、`待辦清單.md` 或歷史文件。
> 若本文件與實際檔案樹衝突，以實際檔案為準，並將衝突列入更新驗證。

## 1. 當前定位

Ai-knowledge 是一套供單人工作室長期使用的 AI 工程知識庫。

核心目的：

> 第一次遇到問題時研究、驗證並保存可重用內容；下一次遇到類似問題時優先重用，降低重新研究與重新理解成本。

核心架構：

```text
知識
↓
系統
↓
方案
↓
軟體／工具
↓
應用
```

「記憶」是維持跨工作階段連續性的內容用途，不等同於知識；目前主要由 `目前狀態.md`、`交接資料.md`、工作紀錄及相關文件共同承載。

## 2. 目前已建立的主要層級

### 知識層 `0-知識/`

目前已形成的主要功能分類：

- 上下文
- 分類
- 搜尋
- 更新
- 比較
- 管理
- 系統
- 證據

其中已建立搜尋／研究相關知識、證據與可信度相關知識、更新相關知識，以及 AI 上下文與交接相關知識。

### 系統層 `1-系統/`

目前已建立或正在使用的主要系統包括：

- 資料分類與組織系統
- 資料探索與搜尋系統
- 來源與證據分析系統
- 知識比較與衝突分析系統
- 知識更新與狀態管理系統
- 知識整合系統
- AI 上下文與交接系統
- 方案管理系統
- 研究問題與搜尋策略系統
- 研究證據與採用判斷系統
- 研究結果整合系統
- 更新影響分析與成本判斷系統

部分系統仍處於「文件已建立、等待或正在實際驗證」狀態；不能因文件存在就視為已完成驗證。

### 方案層 `2-方案/`

目前至少有：

- AI 知識管理方案
- 搜尋研究方案
- 整體系統更新方案（擴展版，正在建立）

「方案」是多個系統為特定目的組合而成的工作方法，不等同於單一系統。

## 3. 目前最重要的工作

目前主線已從「建立單一知識」進入「建立並驗證可重用系統與方案」，並進一步處理整體更新能力。

最近已規劃的整體系統更新方案包含：

- 變更來源與更新觸發判定
- 依使用頻率安排更新檢查
- AI 模型更新檢查
- 新建系統／方案／軟體時重新確認所引用舊知識
- 新舊知識比較與關係判定
- 版本建立與對齊
- 舊版本移入 `廢棄/`
- 直接影響與逐層影響分析
- 更新必要性與更新成本判斷
- 系統／方案同步
- 更新後重新搜尋、確認與一致性驗證
- 更新決策與停止原因記錄

完整功能清單以 `待辦清單.md` 為準。

## 4. 最近狀態

已完成兩輪「無歷史對話、只依 repository 重新接手」測試。

第二輪確認：核心接手架構已大致成立，但仍有三個入口／維護問題：

1. `README.md` 的接手順序落後於目前文件分工。
2. `目前狀態.md` 有被逐輪追加成歷史紀錄的風險。
3. `檔案索引.md` 是所有檔案變更的同步依賴，更新時必須一起維護。

上述問題已在本輪修正：

- `README.md` 現在以 `目前狀態.md` 作為接手入口中的現況文件。
- `README.md` 明確限制 `目前狀態.md` 不承擔測試歷史。
- `目前狀態.md` 保留當前狀態與下一步，不再追加測試過程。
- 後續任何檔案修改仍必須同步進入 `檔案索引.md`。

下一輪測試的目標不是再次只確認「能不能理解 repository」，而是直接執行目前主線的下一個實際工作階段：依 `待辦清單.md` 開始 `整體系統更新方案` 的「功能 → 所需知識」分析，並檢查無記憶 AI 能否正確選擇、搜尋與使用相關文件。

## 5. 文件分工

```text
README.md
= 最短入口

說明.md
= 整體目的、架構與閱讀方式

目前狀態.md
= 現在到底做到哪裡、哪些已成立、哪些仍待驗證

規則.md
= 系統正式怎麼運作

待辦清單.md
= 下一步要做什麼

交接資料.md
= 為什麼走到現在、重要歷史決策與架構轉折

檔案索引.md
= 協助定位近期變更與文件

更新紀錄/
= 已完成更新的歷史記錄
```

任何文件都不應同時承擔上述全部責任。

## 6. 狀態判定原則

文件存在 ≠ 系統已驗證。

系統文件成立 ≠ 方案已驗證。

方案成立 ≠ 實際應用已證明有效。

外部研究成立 ≠ 本 repository 已實際驗證。

目前狀態若無法由文件與實際檔案共同確認，標記為待判定，不自行補完。

## 7. 接手後的最小讀取順序

新 AI 不需要先完整閱讀所有文件。

```text
README.md
↓
說明.md
↓
目前狀態.md
↓
規則.md
↓
待辦清單.md
↓
依當前任務讀取相關系統／方案／知識
```

如果發現以上文件互相矛盾：

```text
停止猜測
↓
檢查實際檔案樹
↓
確認最新提交與更新紀錄
↓
判斷哪一份文件已過時
↓
必要時進入整體系統更新方案
```

## 8. 目前主要未決事項

1. 完成 `整體系統更新方案（擴展版）` 的功能→知識→系統→方案建構。
2. 處理舊 `0-knowledge/` 與正式 `0-知識/` 並存問題。
3. 實際驗證「無記憶 AI 僅依 repository 接手」是否能在有限閱讀下完成下一個任務。
4. 將交接驗證結果回饋至 AI 上下文與交接系統。
5. 後續再處理「融合、疊代」方案；本輪不把融合／疊代混入更新方案。

## 9. 本文件的維護規則

本文件只保存「現在最重要、能直接影響接手與下一步判斷」的資訊。

測試過程、歷史決策與詳細修改紀錄留在 `更新紀錄/` 或既有專門文件，不把本文件變成整個 repository 的摘要副本。

每次重大架構、系統、方案或工作主線變更後，應檢查本文件是否仍然正確。
