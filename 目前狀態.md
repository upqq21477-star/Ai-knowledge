# Ai-knowledge 目前狀態

> 本文件是新 AI 接手時的「目前狀態快照」。
> 它只描述現在，不取代 `規則.md`、`待辦清單.md` 或歷史文件。
> 若本文件與實際檔案樹衝突，以實際檔案為準，並將衝突列入更新驗證。

## 1. 當前定位

Ai-knowledge 是一套供單人工作室長期使用的 AI 工程知識庫。

核心目的：

> 第一次遇到問題時研究、驗證並保存可重用內容；下一次遇到類似問題時優先重用，降低重新研究與重新理解成本。

核心架構：

```text
知識
↓
系統
↓
方案
↓
軟體／工具
↓
應用
```

「記憶」是維持跨工作階段連續性的內容用途，不等同於知識；目前主要由 `目前狀態.md`、`交接資料.md`、工作紀錄及相關文件共同承載。

## 2. 目前正式方案架構

目前正式方案規劃為四個：

```text
1. 整體資料治理方案
2. AI 知識管理方案
3. 知識與資料迭代演化方案
4. 能力蒸餾與系統重構方案
```

其中第 3、4 方案形成循環：

```text
正常迭代
↓
長期累積
↓
蒸餾／重構
↓
新基準
↓
回到正常迭代
```

治理仍是橫向維度，不新增另一個資料層級。

## 3. 本輪主要工作

2026-09-17 已完成本輪兩方案的第一版重製與共同規劃文件建立。

共同規劃：

`2-方案/知識迭代與能力蒸餾方案重製規劃-v1.0.md`

方案 3：

`2-方案/知識與資料迭代演化方案-v2.0.md`

方案 4：

`2-方案/能力蒸餾與系統重構方案-v1.0.md`

本輪核心決策：

- 正常迭代與長期蒸餾拆成兩個責任不同但互相配合的方案。
- 方案 3 負責「這次怎麼安全地改」。
- 方案 4 負責「長期累積後整體是否需要重新整理」。
- 方案 4 不等於 Merge，也不取得所有系統的中央所有權。
- 共同資料語義由整體資料治理方案負責，兩方案不得各自建立另一套 Version、State、Relationship 等共同模型。
- 第一階段不建立大型蒸餾矩陣或自動蒸餾引擎，先以真實案例手動驗證。

## 4. 外部方法比對

本輪重新參考：

- Microsoft 6R application modernization：Rehost、Replatform、Refactor、Rebuild、Retire、Retain。
- AWS portfolio rationalization：以 7R 類型、依賴、複雜度、生命週期與遷移準備度進行逐步 rationalization。
- Backstage Software Catalog：以集中目錄保存身份、Owner、Lifecycle、System、Dependency 等資訊，但不把所有執行邏輯塞進中央目錄。

外部方法只作候選模式來源，不直接複製成 Ai-knowledge 規則。

## 5. 目前方案狀態

### 知識與資料迭代演化方案 v2.0

狀態：

`【建立中；第一版重製完成；待接口交叉檢查與實測】`

核心：

```text
變更訊號
→ 確認
→ 影響分析
→ 演化判定
→ 候選變更
→ 版本／相容性
→ 最小實測
→ 採用／暫緩／退回
→ 回寫
```

### 能力蒸餾與系統重構方案 v1.0

狀態：

`【建立中；第一版完成；待接口交叉檢查與實測】`

核心：

```text
觸發
→ 界定範圍
→ 能力盤點
→ 重疊／依賴分析
→ 候選處置
→ 影響分析
→ 重構／融合／拆分／取代等
→ 實測
→ 新基準
```

第一階段不建立蒸餾引擎。

## 6. 與既有架構的關係

### 與整體資料治理方案

共同資料治理負責：

```text
資料語義
Metadata
State
Version
Relationship
Lifecycle
Traceability
```

兩個新方案使用共同規則，不自行重複定義。

### 與 AI 知識管理方案

AI 實際使用可產生：

```text
過期
缺漏
衝突
Context 過長
重複
使用失敗
```

這些可以成為迭代或蒸餾訊號，但單次使用失敗不直接等於結構性問題。

### 與問題解決系統

若發現能力缺口或重大異常，進入既有問題解決流程，不因方案存在就直接建立新系統。

### 與實測結果回寫

兩方案均必須使用：

```text
預期
→ 實際觸發
→ 實際執行
→ 實際結果
→ 比較
→ 判定
→ 回寫
```

## 7. 第一個真實蒸餾案例

候選案例：

1. `0-knowledge/` 與正式 `0-知識/` 並存。
2. `交接資料.md` 與不同版本交接文件並存。
3. 既有系統中已知但未完成整合的重疊能力。

目前尚未選定。

選擇條件：

```text
範圍有限
可追溯
可回復
不影響核心服務
可完整觀察蒸餾流程
```

## 8. 目前共同資料基礎

仍在施工：

- 共同資料類型交叉矩陣
- Metadata 邊界
- 關係語義字典
- 狀態模型
- 版本模型
- 資料／能力物件共同契約
- 跨方案資料接口
- 跨方案責任接口

目前仍禁止因方案建立就直接凍結完整 JSON／YAML Schema。

## 9. 統御分支候選方案

統御仍為候選橫向能力，不視為第四正式方案。

目前已有：

- System Catalog／Identity 方向
- Trigger Registry 方向
- 啟動保險、冪等性、重入、循環防護與多重匹配處理

仍待 Repository 既有能力逐項比對及自然工作實測。

## 10. 舊文件處理

舊 `知識迭代與資料演化方案-v1.0.md` 不覆寫、不刪除。

它作為歷史設計輸入；新 v2.0 重新建立目前責任邊界。

本輪所有舊能力與舊文件都遵循：

```text
保留歷史
≠
目前採用
```

## 11. 目前禁止事項

- 不批量建立新系統。
- 不建立大型蒸餾引擎。
- 不先建立固定大型蒸餾矩陣。
- 不自動刪除正式資料、系統或方案。
- 不把蒸餾等同於 Merge。
- 不把規模拆分與語義拆分混為一談。
- 不把外部 6R／7R 直接複製為本專案規則。
- 不以文件存在宣稱實際能力已驗證。

## 12. 下一步

```text
兩方案責任交叉檢查
↓
共同資料接口檢查
↓
與整體資料治理／AI知識管理／問題解決／實測／統御候選能力對接
↓
選定第一個低風險真實蒸餾案例
↓
手動完整執行
↓
記錄實際需要的分析維度
↓
實測回寫
↓
再決定是否建立蒸餾小系統／系統
```

文件存在、方案建立、系統成立、實際驗證仍保持分離。
