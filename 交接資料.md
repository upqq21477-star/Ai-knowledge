# AI 知識系統交接資料

> 本文件保存「為什麼走到現在」的歷史背景、重要決策與架構轉折。
> 它不再承擔目前狀態快照；目前狀態請讀 `目前狀態.md`。
> 正式操作規則以 `規則.md` 為準。

## 1. 建立背景

Ai-knowledge 的最初目的，是讓 AI 在第一次遇到問題時研究、整理並保存可重用內容，下一次遇到類似問題時降低重新研究成本。

早期曾把大量注意力放在索引、探索工具與資料結構；後來實際驗證發現，AI 已能直接讀取 repository，因此沒有必要預先建立大型索引或複雜資料庫。

這形成目前的重要原則：

> 先建立真正需要的知識與方法，再由實際使用結果決定是否增加工具或結構。

## 2. 架構演進

早期主要以「規則 → 知識 → 系統／應用」理解。

隨著實際建立多個系統與研究方案，架構逐步明確為：

```text
知識／記憶
↓
系統
↓
方案
↓
軟體／工具
↓
應用
```

新增「方案」是因為單一系統與多系統為特定目的組合之間存在明確差異。

新增「記憶」概念則是為了區分可重用知識與跨工作階段需要保存的個人／專案／狀態脈絡。

## 3. 知識到系統的轉折

早期先建立基礎知識，再確認哪些內容可以形成獨立功能系統。

目前已形成的系統化方向包括：

- 分類與組織
- 探索與搜尋
- 來源與證據
- 比較與衝突分析
- 更新與狀態管理
- 知識整合
- AI 上下文與交接
- 方案管理
- 研究問題與搜尋策略
- 研究證據與採用判斷
- 研究結果整合
- 更新影響與成本判斷

這些系統不能因為文件已建立就直接視為完成；實際驗證仍是成立條件之一。

## 4. 搜尋研究方案的歷史角色

搜尋研究方案原本是為了解決「如何有系統地研究外部資料」而建立。

在後續工作中，它又被拿來研究「如何建立下一個方案」，形成遞迴驗證：

```text
搜尋研究方案
↓
研究如何建立更新方案
↓
建立更新方案
↓
實際執行
↓
發現問題
↓
回饋知識／系統／方案
```

這是建構方法的驗證案例，不表示所有未來方案都必須使用搜尋研究方案。

## 5. 更新系統的形成背景

實際工作中發現，知識更新不是「改一個檔案」這麼簡單。

一個知識改變可能影響：

```text
知識
↓
系統
↓
方案
↓
軟體／應用
```

因此形成「更新影響分析與成本判斷系統」，並進一步擴展為整體系統更新方案。

後續又加入：

- 新舊版本比較
- 語義版本號
- 舊版本移入 `廢棄/`
- 依使用頻率安排更新檢查
- AI 模型每日更新檢查
- 建立新系統／方案／軟體時重新確認外部新知識
- 更新後重新搜尋與一致性驗證

## 6. 無記憶 AI 接手測試

曾進行一次重要測試：假設新 AI 完全沒有歷史對話，只給它 repository，要求它恢復工作狀態。

測試結果不是「資料不存在」，而是「資料存在但入口不夠明確」。主要問題：

1. `交接資料.md` 落後於目前實際進度。
2. `說明.md` 保存了過時的目前階段描述。
3. `規則.md` 與新版本／`廢棄/` 管理方向衝突。
4. 缺少單一可靠的目前狀態快照。
5. 舊 `0-knowledge/` 與正式 `0-知識/` 並存，增加接手時的歧義。

這次測試直接促成 `目前狀態.md` 的建立，並重新定義本文件的職責。

## 7. 已明確淘汰的方向

不要因為 AI 能力提升就預先建立：

- 大型資料庫
- 複雜知識圖譜
- 固定分數式可信度模型
- 固定停止分數
- 自動全庫更新
- 為了形式完整而建立過多索引與註冊表

是否增加這些結構，必須由實際痛點與驗證結果決定。

## 8. 目前接手方式

歷史文件只用來回答：

> 為什麼現在會變成這樣？

如果問題是：

> 現在做到哪裡？

讀 `目前狀態.md`。

如果問題是：

> 現在下一步做什麼？

讀 `待辦清單.md`。

如果問題是：

> 系統正式規則是什麼？

讀 `規則.md`。

如果問題是：

> 為什麼採用這個架構？

再讀本文件。
