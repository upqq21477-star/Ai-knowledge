# 系統操作規則

> 本文件只規定「怎麼做」。目前狀態、設計背景與歷史決策見 `交接資料.md`。本文件是正式操作規則的最高權威。

## 1. 層級定義

- **規則**：定義系統怎麼運作，不保存知識本身。
- **知識**：可跨場景重用、可獨立理解的基本原理、知識、判斷或方法。
- **系統**：由多份知識組合而成，且已在實際工作中證明具有重複使用價值的方法或模型。
- **應用**：針對具體需求，組裝知識或系統形成的實際應用。

核心界線：規則不保存知識本身；知識不預先建立固定關係；應用可以包含專案特有決策；各層更新不自動強制連動。

## 2. 資料探索

**實際檔案樹是內容的唯一真實來源。**

AI 可以直接讀取資料夾，因此不需要依賴索引才能找到內容。

任務導向時只讀相關文件；完整稽核時可以遞迴讀取全部相關文字文件。不能因為沒有讀到某文件，就假設該文件不存在。

## 3. 文件引用

跨文件引用使用 Markdown 相對路徑，例如：

```markdown
[知識甲](../0-knowledge/知識甲.md)
```

不因為 AI 可以直接讀取資料夾，就建立額外索引、登錄表或探索系統。

## 4. 知識格式

檔名使用中文，放在 `0-knowledge/`。

```markdown
# 名稱

> 一句話：解決什麼問題、什麼情境會用到、什麼情況不需要

## 核心原理

## 適用情境

## 不適用情境

## 實務方法／注意事項

## 參考資料

## 我的補充
```

知識應能獨立理解。可以引用其他知識，但核心內容不能依賴其他文件才能成立。

單一專案特殊設定、一次性錯誤修法、只對單一情境有效的決定、未驗證原始資料，不直接建立為正式知識。

若某份知識主要用來補償特定模型能力限制，可以簡單標註「模型能力補償」。模型能力改善後，在實際使用該知識時再判斷是否淘汰，不建立預先的生命週期系統。

## 5. 系統格式

檔名使用中文，放在 `1-systems/`。

建立門檻：至少兩份知識已在同一類問題中實際重複組合使用，並證明這個組合值得保存。只有「可能一起使用」不算。

## 6. 應用格式

放在 `2-applications/`，可以是單一文件或資料夾。

應用可以包含專案特有決策。只有當某個應用的子機制在不同應用中反覆出現，才評估是否抽成系統。

## 7. 更新規則

各層各自判斷，互不強制連動。

知識只有在原本有錯誤、新證據改變核心結論，或實際使用發現重大限制時更新。若屬模型能力補償，且實際使用確認原問題已不存在，才考慮淘汰。

系統只有在組合邏輯受到影響，或實際使用發現更好的組合方式時更新。

應用依具體專案需求更新。

## 8. 最新資料

只有問題具有時效性時才查最新資料，例如模型能力、函式庫版本或平台規格。

## 9. 使用回饋

- 只對這次有用 → 留在該應用。
- 新的可重複組合方式 → 評估建立或更新系統。
- 新的可跨場景基本原理 → 評估建立或更新知識。
- 發現整套系統運作方式有問題 → 評估修改規則。

## 10. 文件與淘汰

檔名保持穩定，不加入版本、最終版、舊版等後綴；版本交給 Git。

淘汰文件時，如果不需要保存歷史脈絡可直接刪除；只有真的需要保存歷史時，才建立封存資料夾。

## 11. 目前不預先建立

目前不加入：知識編號、正式來源分級、文件版本歷史表、規格遷移流程、能力登錄表、正式衝突處理流程、知識圖譜、依賴資料庫、自動連鎖更新、複雜檢索系統、多層上下文載入、原子知識、高階知識聚合、複雜狀態機、能力生命週期、大型自動化知識流程、預先封存資料夾、獨立執行環境。

這些不是待辦事項。只有實際問題出現後才重新評估。

## 12. 新機制加入前

任何新機制先問：

> **它是否直接降低下一次重新研究問題的成本？**

如果沒有，不加入。如果有，先尋找最小解法，優先使用 Markdown、Git 與現有檔案結構。
