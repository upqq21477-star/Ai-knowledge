# AI 個人工程知識系統

這是一套供單人工作室長期使用的 AI 工程知識系統。

目的不是保存最多資料，而是把經過理解、比較、外部比對、實際驗證的內容，逐步形成可重用的知識、系統與方案，降低未來重新研究與重新理解的成本。

## 新 AI 接手入口

請依序讀取：

```text
README.md
↓
說明.md
↓
目前狀態.md
↓
規則.md
↓
待辦清單.md
↓
依目前任務讀取相關系統／方案／知識
```

其中：

- `目前狀態.md` 是現在狀態的第一判斷入口。
- `規則.md` 是正式操作規則。
- `待辦清單.md` 是下一步工作入口。
- `交接資料.md` 是歷史背景與重要決策，不是目前狀態快照。
- `檔案索引.md` 用於定位近期文件與變更，不是完整狀態資料庫。

如果上述文件互相矛盾，先檢查實際檔案樹與更新紀錄，不要自行猜測。

## 核心架構

```text
知識／記憶
↓
系統
↓
方案
↓
軟體／工具
↓
應用
```

知識、記憶、系統、方案、軟體與應用是不同概念：

- **知識**：可在明確情境與適用條件下重用的內容。
- **記憶**：維持跨工作階段連續性的專案、決策、歷史或目前狀態資訊。
- **系統**：可獨立處理一類問題的方法結構。
- **方案**：為特定目的組合多個系統與條件的工作方法。
- **軟體**：把系統實際實作與整合的工程載體。
- **應用**：具體專案或需求中的使用情境。

## 目前目錄

正式中文主架構：

```text
0-知識/
1-系統/
2-方案/
2-軟體/
3-應用/
```

`0-知識/` 採功能導向分類，原則上最多兩層：

```text
0-知識/功能分類/原理文件.md
```

目前已有的主要功能分類包括：

```text
上下文／分類／搜尋／更新／比較／管理／系統／證據
```

目前 repository 仍存在舊的 `0-knowledge/` 英文目錄。它是結構遺留，不應與正式 `0-知識/` 自動視為同一層；後續更新工作需要明確處理其保留、遷移或淘汰。

## 系統層

目前已建立或正在驗證的系統包括：

- 資料分類與組織系統
- 資料探索與搜尋系統
- 來源與證據分析系統
- 知識比較與衝突分析系統
- 知識更新與狀態管理系統
- 知識整合系統
- AI 上下文與交接系統
- 方案管理系統
- 研究問題與搜尋策略系統
- 研究證據與採用判斷系統
- 研究結果整合系統
- 更新影響分析與成本判斷系統

系統文件存在不等於系統已完成實際驗證。

## 方案層

目前主要方案：

- `AI 知識管理方案`
- `搜尋研究方案`
- `整體系統更新方案（擴展版）`（正在建立）

搜尋研究方案曾被用來研究「如何建立下一個方案」，因此它同時是研究工具與第一輪方案建構驗證案例；這不代表所有未來方案都必須透過它建立。

## 外部研究原則

建立新的重要知識、系統或方案前，應優先搜尋外部成熟方法、工具與實務做法，再與現有內容比較。

外部資料是形成與檢查知識的依據，不直接等同於本 repository 的正式知識。

## 更新原則

目前正在建立的整體系統更新方案包含：

```text
變更偵測
↓
更新觸發
↓
新舊內容比較
↓
版本與狀態判定
↓
直接／逐層影響分析
↓
更新必要性與成本判斷
↓
更新
↓
重新搜尋／確認
↓
一致性驗證
```

新版本可使用語義版本號：小幅補充／修正可使用 `v1.x`；核心定義或適用邊界實質改變時進入 `v2.0`。

舊版本若需要保留，移入同層 `廢棄/`。Git history 仍保留完整提交歷史；兩者用途不同。

## AI 交接的核心原則

真正需要保存的不是整段對話，而是能讓下一個 AI 重建必要工作狀態的最小充分資訊。

外部近期實務也強調將決策、限制與目前狀態從短暫對話外化為 repository 中的持續文件；這與本專案把「目前狀態」獨立成入口的方向一致。citeturn0search0turn0search2

因此本專案採：

```text
穩定架構
＋
目前狀態快照
＋
待辦
＋
歷史決策
＋
專門知識／系統／方案
```

而不是建立一份越來越大的「全部交接文件」。

## 驗證原則

必須區分：

```text
文件存在
≠
知識成立
≠
系統成立
≠
方案成立
≠
實際任務有效
```

本 repository 的下一步由 `待辦清單.md` 決定；目前狀態由 `目前狀態.md` 決定；正式做法由 `規則.md` 決定。
