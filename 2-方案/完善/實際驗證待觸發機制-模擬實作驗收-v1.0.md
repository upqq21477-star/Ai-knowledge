# 實際驗證待觸發機制－模擬實作驗收 v1.0

日期：2026-09-18  
來源問題：GitHub Issue #1  
狀態：【SIMULATION-PASSED；待模擬回歸】

## 一、驗收目的

驗證新建立的「待實際驗證 → 自然觸發 → Observation → 正常／異常／無法判定」機制，是否解決原問題，且沒有把 FIELD 變成硬 Gate 或建立第二套狀態模型。

## 二、驗收案例

| ID | 情境 | 預期 | 結果 |
|---|---|---|---|
| T01 | 模擬驗收完成但沒有自然案例 | 保持 FIELD-PENDING，不阻塞 | PASS |
| T02 | 工程自然使用 pending 能力 | 觸發 Observation Event | PASS |
| T03 | 自然使用結果正常 | 清除 pending，保留歷史 | PASS |
| T04 | 自然使用結果異常 | 保留／重開 pending 並建立問題事件 | PASS |
| T05 | 證據不足 | INCONCLUSIVE，保留 pending | PASS |
| T06 | 嘗試人工製造案例只為關閉 pending | 被規則禁止 | PASS |
| T07 | SIMULATION-PASSED + FIELD-PENDING | 仍可正常工程 | PASS |
| T08 | 單一 FIELD 異常 | 不直接判定整體方案失敗 | PASS |
| T09 | 單一 FIELD 異常 | 不自動啟動 Scheme 4 | PASS |
| T10 | 正常 Observation | 歷史紀錄不可因清除 pending 而消失 | PASS |
| T11 | 後續再次發生異常 | 建立新 Observation／問題事件，不修改舊成功紀錄 | PASS |
| T12 | 版本變更後再次使用 | Observation 綁定當時 Version / State | PASS |

## 三、負向驗收

### N01：FIELD-PENDING 阻塞工程
結果：PASS（禁止）。

### N02：SIMULATION-PASSED 直接改成 FIELD-PASSED
結果：PASS（禁止）。

### N03：INCONCLUSIVE 當作 NORMAL
結果：PASS（禁止）。

### N04：清除 pending 時刪除 Observation
結果：PASS（禁止）。

### N05：單一異常直接進 Scheme 4
結果：PASS（禁止）。

### N06：新增平行生命週期
結果：PASS（禁止；FIELD-OBSERVING 僅為事件處理標記）。

## 四、跨方案驗收

1. 方案一：Target 可引用 Stable Identity / Version / State / Evidence。
2. 方案二：自然任務可作為 Trigger / Context。
3. 方案三：版本與變更可作為 Observation 的追溯條件。
4. 方案四：異常可成為 D20 長期觀察或重新蒸餾的證據來源，但不自動觸發。
5. 問題解決系統：ABNORMAL 可直接建立問題解決事件。

結果：PASS。

## 五、驗收結論

12/12 正向案例 PASS。  
6/6 負向案例 PASS。  
4/4 跨方案接口 PASS。  
阻塞性缺口：0。

因此：

```text
問題研究 PASS
候選解法選定 PASS
最小修改 PASS
模擬實作驗收 PASS
→ 進入模擬回歸
```
